# Review rules for this project

<!-- Yours to write. Anything here beats the stack templates and everything in memory.md.
     Delete this comment and start writing — plain sentences are fine, no format required.
     Examples of what belongs here:
       - Never call the payment gateway directly from a controller, go through BillingService.
       - Migrations must be reversible; a destructive one needs a written rollback plan.
       - We allow `any` in test files, not in src/. -->
