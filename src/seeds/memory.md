<!-- Index. One line per entry, no repeated words:
     - [tag] [short label](path) — 1-line hook
     `path` → `memories/<slug>.md` (a lesson) OR a path INSIDE this repo (a pointer to the project's
     own convention — never a copy of its content). Several tags if it spans stacks, e.g.
     [rails][ruby]. Write the rule, not its story: no history, no bug report, no ticket id. Merge
     duplicates; don't restate "see the convention at…", the link already says it. -->
