<!-- Index. One entry per line, concise, no repeated words:
     - [tag] [short label](path) — a 1-line hook
     `path` → `memories/<slug>.md` (a self-learned lesson) OR a path INSIDE this repo (a pointer to
     the project's own convention — never a copy of its content). Several tags if it spans stacks,
     e.g. [rails][ruby]. ≤1 sentence per line; merge duplicates; don't restate "see the convention
     at…", the link already says it. -->
